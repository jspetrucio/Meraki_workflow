# Story 10.6: Wireless Health Monitoring

> **Epic:** 10 - Advanced Switching, Wireless & Platform
> **Sprint:** S9
> **Priority:** P0 (core operations)
> **Points:** 5
> **Type:** Backend/Python
> **Agent:** @dev
> **Depends on:** None (independent)

---

## Status

**Draft**

---

## Story

**As a** network analyst,
**I want** comprehensive wireless health metrics,
**so that** I can diagnose Wi-Fi issues proactively.

---

## Acceptance Criteria

1. `discover_wireless_health` returns aggregated health metrics per network
2. Metrics include: connection success rate, avg latency, signal quality scores
3. `get_channel_utilization` returns per-AP channel utilization data
4. `get_failed_connections` returns failed auth/assoc/DHCP breakdown
5. Integrated into discovery reports with health summary

---

## Scope

| Layer | New Items | Details |
|-------|-----------|---------|
| `api.py` | 5 methods | `get_wireless_connection_stats`, `get_wireless_latency_stats`, `get_wireless_signal_quality`, `get_channel_utilization`, `get_failed_connections` |
| `discovery.py` | 1 function | `discover_wireless_health` |
| `agent_tools.py` | 5 schemas | network-analyst tools |
| Safety | SAFE | Read-only |

**Note:** This story does not require config.py functions -- read-only monitoring.

---

## Implementation Pattern

This story follows a read-only implementation pattern (no config layer):

1. `api.py` -- Add SDK wrapper methods with `@log_api_call`
2. `discovery.py` -- Add discovery function with `client.safe_call()`
3. `agent_router.py` -- Register in `FUNCTION_REGISTRY`
4. `agent_tools.py` -- Add tool schemas + TOOL_SAFETY classification
5. `safety.py` -- Register in `SAFETY_CLASSIFICATION`
6. `tests/` -- Add comprehensive tests

**Note:** Both `SAFETY_CLASSIFICATION` (safety.py) and `TOOL_SAFETY` (agent_tools.py) must be updated.

---

## Tasks

- [ ] Add `get_wireless_connection_stats`, `get_wireless_latency_stats`, `get_wireless_signal_quality`, `get_channel_utilization`, `get_failed_connections` to api.py with `@log_api_call`
- [ ] Add `discover_wireless_health` to discovery.py with `client.safe_call()`
- [ ] Register wireless health functions in FUNCTION_REGISTRY (agent_router.py)
- [ ] Add 5 tool schemas to agent_tools.py (network-analyst)
- [ ] Register safety classifications in safety.py (SAFE) and agent_tools.py (TOOL_SAFETY)
- [ ] Integrate wireless health into discovery reports
- [ ] Add tests (target: >= 90% coverage)
- [ ] Verify all schemas pass `validate_tool_schema()` with `additionalProperties: False`

---

## File List

(To be populated during implementation)

---
