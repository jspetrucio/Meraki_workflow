# Story 13.2: Sensor Readings & Alerts

> **Epic:** 13 - Camera Analytics, Sensors & Observability
> **Sprint:** TBD
> **Priority:** P1 (platform depth)
> **Points:** 5
> **Type:** Backend/Python
> **Agent:** @dev
> **Depends on:** None (independent)

---

## Status

**Draft**

---

## Story

**As a** facilities engineer,
**I want** to view sensor readings and configure alerts,
**so that** I can monitor environmental conditions (temperature, humidity, etc.).

---

## Acceptance Criteria

1. `discover_sensors` returns all MT sensors with latest readings
2. `get_sensor_readings_history` returns time-series data for a sensor
3. `create_sensor_alert` configures threshold-based alerts
4. Issue detection: `sensor_alert_missing` when no alerts on critical sensors
5. Backup created before any sensor alert config change
6. Integration into `full_discovery()` output
7. Natural language: "show me sensor readings" or "create temperature alert for sensor X" works

---

## Scope

| Layer | New Items | Details |
|-------|-----------|---------|
| `api.py` | 4 methods | `get_sensor_readings_latest`, `get_sensor_readings_history`, `get_sensor_alert_profiles`, `create_sensor_alert` |
| `discovery.py` | 1 function | `discover_sensors` |
| `config.py` | 1 function | `configure_sensor_alert` |
| `agent_tools.py` | 4 schemas | network-analyst + meraki-specialist tools |
| Safety | SAFE (read) / MODERATE (alert config) |

---

## Implementation Pattern

This story follows the standardized 7-step implementation pattern:

1. `api.py` -- Add SDK wrapper methods with `@log_api_call`
2. `discovery.py` -- Add discovery function with `client.safe_call()`
3. `config.py` -- Add config function with backup + `ConfigResult`
4. `agent_router.py` -- Register in `FUNCTION_REGISTRY`
5. `agent_tools.py` -- Add tool schemas + TOOL_SAFETY classification
6. `safety.py` -- Register in `SAFETY_CLASSIFICATION`
7. `tests/` -- Add comprehensive tests

**Note:** Both `SAFETY_CLASSIFICATION` (safety.py) and `TOOL_SAFETY` (agent_tools.py) must be updated.

---

## Tasks

- [ ] Add `get_sensor_readings_latest`, `get_sensor_readings_history`, `get_sensor_alert_profiles`, `create_sensor_alert` to api.py with `@log_api_call`
- [ ] Add `discover_sensors` to discovery.py with `client.safe_call()`
- [ ] Add hardware product-type guard for MT sensors (skip if network has no `sensor` product)
- [ ] Add `configure_sensor_alert` to config.py with backup + `ConfigResult`
- [ ] Register sensor functions in FUNCTION_REGISTRY (agent_router.py)
- [ ] Add 4 tool schemas to agent_tools.py (network-analyst + meraki-specialist)
- [ ] Register safety classifications in safety.py (SAFE for reads, MODERATE for alert config) and agent_tools.py (TOOL_SAFETY)
- [ ] Add `sensor_alert_missing` issue detection to `find_issues()`
- [ ] Integrate sensor readings into `full_discovery()` output
- [ ] Add tests (target: >= 90% coverage)
- [ ] Verify all schemas pass `validate_tool_schema()` with `additionalProperties: False`

---

## File List

(To be populated during implementation)

---

## Dependencies

- Requires MT sensor devices in network
- MT device licenses must be active
- Sensor polling must be enabled in Dashboard

---

## Testing

### Expected Test Count: ~10-14 tests

**Test Coverage:**
- `test_get_sensor_readings_latest` - API method
- `test_get_sensor_readings_history` - API method
- `test_get_sensor_alert_profiles` - API method
- `test_create_sensor_alert` - API method
- `test_discover_sensors` - Discovery function
- `test_configure_sensor_alert` - Config function with backup
- `test_sensor_product_guard` - Hardware guard (skip when no MT)
- `test_sensor_alert_missing_issue` - Issue detection
- `test_function_registry_sensors` - Router registration
- `test_sensor_tool_schemas` - Schema validation
- `test_sensor_safety_classification` - Safety classification (SAFE + MODERATE)

---
