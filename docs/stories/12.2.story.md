# Story 12.2: 802.1X / Access Policies

> **Epic:** 12 - Switching, Wireless & Monitoring
> **Sprint:** TBD
> **Priority:** P1 (security operations)
> **Points:** 5
> **Type:** Backend/Python
> **Agent:** @dev
> **Depends on:** Phase 1 complete (Epics 8-10)

---

## Status

**Draft**

---

## Story

**As a** security engineer,
**I want** to configure 802.1X access policies on switch ports,
**so that** I can enforce NAC on wired connections through natural language.

---

## Acceptance Criteria

1. `discover_access_policies` returns all policies with RADIUS server configs
2. `configure_access_policy` supports MAB, 802.1X, and hybrid modes
3. RADIUS server configuration included in policy setup
4. Issue detection: `access_policy_missing` when no NAC on trunk ports
5. Backup created before any access policy change
6. Dry-run mode supported: preview policy changes before applying
7. Access policy status integrated into `full_discovery()` output
8. Full CRUD for access policies (create, update, delete)

---

## Scope

| Layer | New Items | Details |
|-------|-----------|---------|
| `api.py` | 4 methods | `get_access_policies`, `create_access_policy`, `update_access_policy`, `delete_access_policy` |
| `discovery.py` | 1 function | `discover_access_policies` |
| `config.py` | 1 function | `configure_access_policy` |
| `agent_tools.py` | 3 schemas | meraki-specialist tools |
| `safety.py` | MODERATE | Affects port authentication |

---

## Implementation Pattern

This story follows the standardized 7-step implementation pattern:

1. `api.py` -- Add SDK wrapper methods with `@log_api_call`
2. `discovery.py` -- Add discovery function with `client.safe_call()`
3. `config.py` -- Add config function with backup + `ConfigResult`
4. `agent_router.py` -- Register in `FUNCTION_REGISTRY`
5. `agent_tools.py` -- Add tool schemas + TOOL_SAFETY classification
6. `safety.py` -- Register in `SAFETY_CLASSIFICATION`
7. `tests/` -- Add comprehensive tests

**Note:** Both `SAFETY_CLASSIFICATION` (safety.py) and `TOOL_SAFETY` (agent_tools.py) must be updated.

---

## Tasks

- [ ] Add `get_access_policies`, `create_access_policy`, `update_access_policy`, `delete_access_policy` to api.py with `@log_api_call`
- [ ] Add `discover_access_policies` to discovery.py with `client.safe_call()`
- [ ] Add `configure_access_policy` to config.py with backup + `ConfigResult`
- [ ] Register access policy functions in FUNCTION_REGISTRY (agent_router.py)
- [ ] Add 3 tool schemas to agent_tools.py (meraki-specialist)
- [ ] Register safety classifications in safety.py (MODERATE) and agent_tools.py (TOOL_SAFETY)
- [ ] Add `access_policy_missing` issue detection to `find_issues()`
- [ ] Integrate access policy status into `full_discovery()` output
- [ ] Implement dry-run mode for policy changes
- [ ] Add tests (target: >= 90% coverage)
- [ ] Verify all schemas pass `validate_tool_schema()` with `additionalProperties: False`

---

## File List

(To be populated during implementation)

---

## Dependencies

- Phase 1 complete (Epics 8-10)
- Switch devices must be present in network (product type guard: `switch`)
- 802.1X requires RADIUS server configuration
- Access policies require MS firmware >= 11.x

---

## Testing

**Target:** >= 90% coverage

**Test Files:**
- `tests/test_epic12_switching.py`

**Test Scenarios:**
1. API wrappers: verify SDK calls and `@log_api_call` decorator
2. Discovery: verify policy data structure with RADIUS servers
3. Config: verify backup creation before policy changes
4. Issue detection: verify `access_policy_missing` detection logic
5. Safety: verify MODERATE classification
6. Dry-run: verify preview mode without applying changes
7. Product type guard: verify early-exit when no switches present
8. Integration: verify access policy status in `full_discovery()` output
9. CRUD: verify create, update, delete operations

---
