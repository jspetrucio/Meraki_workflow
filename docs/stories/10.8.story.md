# Story 10.8: Org Admins Management

> **Epic:** 10 - Advanced Switching, Wireless & Platform
> **Sprint:** S9
> **Priority:** P0 (core operations)
> **Points:** 3
> **Type:** Backend/Python
> **Agent:** @dev
> **Depends on:** None (independent)

---

## Status

**Draft**

---

## Story

**As a** org admin,
**I want** to manage Dashboard admin accounts via CNL,
**so that** I can provision and audit admin access.

---

## Acceptance Criteria

1. `discover_org_admins` returns all admins with roles and 2FA status
2. `create_admin` provisions a new admin with specified role
3. `delete_admin` removes an admin (requires explicit confirmation)
4. `manage_admin` updates role/permissions
5. Guard-rail: Cannot delete the last remaining org admin
6. Guard-rail: Cannot delete the admin account associated with the current API key

---

## Scope

| Layer | New Items | Details |
|-------|-----------|---------|
| `api.py` | 4 methods | `get_admins`, `create_admin`, `update_admin`, `delete_admin` |
| `discovery.py` | 1 function | `discover_org_admins` |
| `config.py` | 1 function | `manage_admin` |
| `agent_tools.py` | 3 schemas | meraki-specialist tools |
| Safety | DANGEROUS | Admin management is security-sensitive |

---

## Implementation Pattern

This story follows the standardized 7-step implementation pattern:

1. `api.py` -- Add SDK wrapper methods with `@log_api_call`
2. `discovery.py` -- Add discovery function with `client.safe_call()`
3. `config.py` -- Add config function with backup + `ConfigResult`
4. `agent_router.py` -- Register in `FUNCTION_REGISTRY`
5. `agent_tools.py` -- Add tool schemas + TOOL_SAFETY classification
6. `safety.py` -- Register in `SAFETY_CLASSIFICATION`
7. `tests/` -- Add comprehensive tests

**Note:** Both `SAFETY_CLASSIFICATION` (safety.py) and `TOOL_SAFETY` (agent_tools.py) must be updated.

---

## Tasks

- [ ] Add `get_admins`, `create_admin`, `update_admin`, `delete_admin` to api.py with `@log_api_call`
- [ ] Add `discover_org_admins` to discovery.py with `client.safe_call()`
- [ ] Add `manage_admin` to config.py with backup + `ConfigResult`
- [ ] Implement guard-rail: prevent deleting the last remaining org admin
- [ ] Implement guard-rail: prevent deleting the admin associated with the current API key
- [ ] Register admin management functions in FUNCTION_REGISTRY (agent_router.py)
- [ ] Add 3 tool schemas to agent_tools.py (meraki-specialist)
- [ ] Register safety classifications in safety.py (DANGEROUS) and agent_tools.py (TOOL_SAFETY)
- [ ] Add tests (target: >= 90% coverage)
- [ ] Verify all schemas pass `validate_tool_schema()` with `additionalProperties: False`

---

## File List

(To be populated during implementation)

---
