# Story 12.1: Switch Stacks

> **Epic:** 12 - Switching, Wireless & Monitoring
> **Sprint:** TBD
> **Priority:** P1 (enterprise operations)
> **Points:** 5
> **Type:** Backend/Python
> **Agent:** @dev
> **Depends on:** Phase 1 complete (Epics 8-10)

---

## Status

**Draft**

---

## Story

**As a** network engineer,
**I want** to manage switch stacks via CNL,
**so that** I can configure stack members and routing through natural language.

---

## Acceptance Criteria

1. `discover_switch_stacks` returns all stacks with member serials and roles
2. Stack CRUD operations supported (create, add member, remove member)
3. `get_stack_routing` returns routing interfaces for stack
4. Safety: DANGEROUS classification for add/remove operations
5. Issue detection: `stp_inconsistent` when STP configuration mismatches across stack members
6. Backup created before any stack config change
7. Dry-run mode supported: preview stack changes before applying
8. Stack status integrated into `full_discovery()` output

---

## Scope

| Layer | New Items | Details |
|-------|-----------|---------|
| `api.py` | 5 methods | `get_switch_stacks`, `create_switch_stack`, `add_to_stack`, `remove_from_stack`, `get_stack_routing` |
| `discovery.py` | 1 function | `discover_switch_stacks` |
| `config.py` | 1 function | `manage_switch_stack` |
| `agent_tools.py` | 4 schemas | meraki-specialist tools |
| `safety.py` | DANGEROUS | Stack changes can cause outages |

---

## Implementation Pattern

This story follows the standardized 7-step implementation pattern:

1. `api.py` -- Add SDK wrapper methods with `@log_api_call`
2. `discovery.py` -- Add discovery function with `client.safe_call()`
3. `config.py` -- Add config function with backup + `ConfigResult`
4. `agent_router.py` -- Register in `FUNCTION_REGISTRY`
5. `agent_tools.py` -- Add tool schemas + TOOL_SAFETY classification
6. `safety.py` -- Register in `SAFETY_CLASSIFICATION`
7. `tests/` -- Add comprehensive tests

**Note:** Both `SAFETY_CLASSIFICATION` (safety.py) and `TOOL_SAFETY` (agent_tools.py) must be updated.

---

## Tasks

- [ ] Add `get_switch_stacks`, `create_switch_stack`, `add_to_stack`, `remove_from_stack`, `get_stack_routing` to api.py with `@log_api_call`
- [ ] Add `discover_switch_stacks` to discovery.py with `client.safe_call()`
- [ ] Add `manage_switch_stack` to config.py with backup + `ConfigResult`
- [ ] Register switch stack functions in FUNCTION_REGISTRY (agent_router.py)
- [ ] Add 4 tool schemas to agent_tools.py (meraki-specialist)
- [ ] Register safety classifications in safety.py (DANGEROUS) and agent_tools.py (TOOL_SAFETY)
- [ ] Add `stp_inconsistent` issue detection to `find_issues()`
- [ ] Integrate switch stack status into `full_discovery()` output
- [ ] Implement dry-run mode for stack changes
- [ ] Add tests (target: >= 90% coverage)
- [ ] Verify all schemas pass `validate_tool_schema()` with `additionalProperties: False`

---

## File List

(To be populated during implementation)

---

## Dependencies

- Phase 1 complete (Epics 8-10)
- Switch devices must be present in network (product type guard: `switch`)
- Stack API support requires MS firmware >= 14.x

---

## Testing

**Target:** >= 90% coverage

**Test Files:**
- `tests/test_epic12_switching.py`

**Test Scenarios:**
1. API wrappers: verify SDK calls and `@log_api_call` decorator
2. Discovery: verify stack data structure with members and roles
3. Config: verify backup creation before stack changes
4. Issue detection: verify `stp_inconsistent` detection logic
5. Safety: verify DANGEROUS classification requires confirmation
6. Dry-run: verify preview mode without applying changes
7. Product type guard: verify early-exit when no switches present
8. Integration: verify stack status in `full_discovery()` output

---
