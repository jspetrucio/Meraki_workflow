# Story 9.8: Change Log Access

> **Epic:** 9 - Alerts, Firmware & Observability
> **Sprint:** S8
> **Priority:** P0 (core operations)
> **Points:** 2
> **Type:** Backend/Python
> **Agent:** @dev
> **Depends on:** None (independent)

---

## Status

**Draft**

---

## Story

**As a** network admin,
**I want** to view recent configuration changes from the Dashboard,
**so that** I can audit who changed what and when.

---

## Acceptance Criteria

1. `get_config_changes` returns org-wide change log with admin, timestamp, and description
2. `discover_recent_changes` filters for last 24h/7d/30d changes
3. Natural language: "show me who changed the firewall rules this week" works

---

## Scope

| Layer | New Items | Details |
|-------|-----------|---------|
| `api.py` | 1 method | `get_config_changes` |
| `discovery.py` | 1 function | `discover_recent_changes` |
| `agent_tools.py` | 1 schema | network-analyst tool |
| Safety | SAFE | Read-only |

---

## Implementation Pattern

This story follows a simplified implementation pattern (no config functions):
1. `api.py` — Add SDK wrapper method with `@log_api_call`
2. `discovery.py` — Add discovery function with `client.safe_call()`
3. `agent_router.py` — Register in `FUNCTION_REGISTRY`
4. `agent_tools.py` — Add tool schema + TOOL_SAFETY classification
5. `safety.py` — Register in `SAFETY_CLASSIFICATION`
6. `tests/` — Add comprehensive tests

**Note:** Both `SAFETY_CLASSIFICATION` (safety.py) and `TOOL_SAFETY` (agent_tools.py) must be updated. No config.py changes needed for this story.

---

## Tasks

- [ ] Add api.py method (`get_config_changes`)
- [ ] Add discovery function to discovery.py (`discover_recent_changes`)
- [ ] Register in FUNCTION_REGISTRY (agent_router.py)
- [ ] Add tool schema to agent_tools.py (1 schema for network-analyst)
- [ ] Register safety classifications in safety.py and agent_tools.py (SAFE)
- [ ] Add tests (target: >= 90% coverage)
- [ ] Verify all schemas pass validate_tool_schema()

---

## File List

(To be populated during implementation)

---
