# Story 11.3: Adaptive Policy / SGT

> **Epic:** 11 - SD-WAN, Policy & Templates
> **Sprint:** S8
> **Priority:** P1 (advanced operations)
> **Points:** 8
> **Type:** Backend/Python
> **Agent:** @dev
> **Depends on:** Story 11.5 (Policy Objects)

---

## Status

**Draft**

---

## Story

**As a** security architect,
**I want** to manage Adaptive Policy (SGT-based) via CNL,
**so that** I can define micro-segmentation policies.

---

## Acceptance Criteria

1. `discover_adaptive_policies` returns all SGT assignments, ACLs, and policies
2. `configure_adaptive_policy` creates/updates policy with source/destination SGTs
3. Uses policy objects from Story 11.5 for reusable IP/FQDN/port references
4. Integration with existing SGT preflight check in config.py
5. Backup created before any Adaptive Policy config change
6. Dry-run mode supported: preview Adaptive Policy changes before applying
7. Adaptive Policy data integrated into `full_discovery()` output

---

## Scope

| Layer | New Items | Details |
|-------|-----------|---------|
| `api.py` | 3 methods | `get_adaptive_policies`, `create_adaptive_policy`, `get_adaptive_policy_acls` |
| `discovery.py` | 1 function | `discover_adaptive_policies` |
| `config.py` | 1 function | `configure_adaptive_policy` |
| `agent_tools.py` | 3 schemas | meraki-specialist tools |
| Safety | DANGEROUS | Policy changes affect segmentation |

---

## Implementation Pattern

This story follows the standardized 7-step implementation pattern:

1. `api.py` -- Add SDK wrapper methods with `@log_api_call`
2. `discovery.py` -- Add discovery function with `client.safe_call()`
3. `config.py` -- Add config function with backup + `ConfigResult`
4. `agent_router.py` -- Register in `FUNCTION_REGISTRY`
5. `agent_tools.py` -- Add tool schemas + TOOL_SAFETY classification
6. `safety.py` -- Register in `SAFETY_CLASSIFICATION`
7. `tests/` -- Add comprehensive tests

**Note:** Both `SAFETY_CLASSIFICATION` (safety.py) and `TOOL_SAFETY` (agent_tools.py) must be updated.

---

## Tasks

- [ ] Add `get_adaptive_policies`, `create_adaptive_policy`, `get_adaptive_policy_acls` to api.py with `@log_api_call`
- [ ] Add `discover_adaptive_policies` to discovery.py with `client.safe_call()`
- [ ] Add `configure_adaptive_policy` to config.py with backup + `ConfigResult`
- [ ] Integrate with policy objects from Story 11.5 (`get_policy_objects`, `create_policy_object`)
- [ ] Register Adaptive Policy functions in FUNCTION_REGISTRY (agent_router.py)
- [ ] Add 3 tool schemas to agent_tools.py (meraki-specialist)
- [ ] Register safety classifications in safety.py (DANGEROUS) and agent_tools.py (TOOL_SAFETY)
- [ ] Verify integration with existing SGT preflight check in config.py
- [ ] Integrate Adaptive Policy data into `full_discovery()` output
- [ ] Implement dry-run mode for Adaptive Policy changes
- [ ] Add tests (target: >= 90% coverage)
- [ ] Verify all schemas pass `validate_tool_schema()` with `additionalProperties: False`

---

## File List

(To be populated during implementation)

---

## Dependencies

- **Story 11.5** (Policy Objects) - MUST be implemented first
- Phase 1 complete (Epics 8-10)
- Requires Adaptive Policy enabled at org level
- Uses existing backup infrastructure from config.py

---

## Testing

### Test Coverage

- Unit tests for all 3 api.py methods
- Integration tests for discovery and config functions
- Safety classification tests
- Tool schema validation tests
- Integration test with policy objects from Story 11.5
- SGT preflight check validation test
- Dry-run mode test

### Test Expectations

- >= 90% code coverage for new functions
- All schemas pass validation with `additionalProperties: False`
- DANGEROUS tools require confirmation
- Backup created before all config changes
- `full_discovery()` includes Adaptive Policy data when feature is enabled
- Policy objects correctly referenced in Adaptive Policy rules

---
